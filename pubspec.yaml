name: dalil_mosafer
description: دليل مسافر برو - ملك عوض الله ربيع - نسخة حقيقية
publish_to: 'none'
version: 2.0.0+6
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  url_launcher: ^6.2.5
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  geolocator: ^11.1.0
  permission_handler: ^11.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
